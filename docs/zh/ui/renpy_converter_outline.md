# 概览
说明该模块将 FairyGUI 资源转换为 Ren'Py 的 screen 语言输出。

# 输入与依赖
说明依赖 `FguiAssetsParseLib` 解析后的资源数据及相关环境配置。

# 枚举与基础常量
说明子组件类型与方向枚举、默认样式与特殊界面名称集合等基础定义。

# 模板与预设加载
说明从 `renpy_templates` 读取图形/图鉴/样式等模板并初始化预设。

# 图像与图形定义生成
说明从图集生成 Ren'Py 图像定义与图形组件定义的流程。

# 组件到屏幕的转换
说明将组件树转换为 screen 结构与 UI 代码的核心逻辑。

# 特殊界面生成
说明对 `choice`、`save/load`、`history`、`say`、`gallery`、`music_room` 等界面的专用生成流程。

# 列表与滚动条样式
说明列表、滚动条与滑块样式生成及相关参数计算。

# 文本与按钮处理
说明文本样式、描边、按钮参数与子组件生成的处理规则。

# 变量与辅助函数
说明变量定义、对齐转换、图像命名等辅助函数的用途。

# 代码输出与文件写入
说明 Ren'Py 代码汇总、文件保存以及预置文件/图集复制。

# 转换入口与清理
说明转换入口函数、资源清理与生命周期处理。
